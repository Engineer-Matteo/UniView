<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vubview">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.VUBView"
        android:usesCleartextTraffic="true">

        <activity android:name=".SettingsActivity"
            android:label="Instellingen" />
        <activity android:name=".ResultsActivity"
            android:label="Resultaten" />
        <activity android:name=".ScheduleActivity"
            android:label="Lesrooster" />
        <activity android:name=".ExamsActivity"
            android:label="Examenrooster" />
        <activity android:name=".CoursesActivity"
            android:label="Opleidingsonderdelen" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>